name: wallhaven_app
description: A luxury wallpaper app for Wallhaven.cc
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  dio: ^5.4.0
  cached_network_image: ^3.3.0
  provider: ^6.1.1
  flutter_staggered_grid_view: ^0.7.0
  url_launcher: ^6.2.2
  path_provider: ^2.1.1
  gallery_saver: ^2.3.2
  share_plus: ^7.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
